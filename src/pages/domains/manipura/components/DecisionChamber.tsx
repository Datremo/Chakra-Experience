import React,{useState}from'react';import{motion,AnimatePresence}from'framer-motion';const o=[['React','The familiar impulse gets the first word.'],['Pause','Create one breath of space before acting.'],['Respond','Act deliberately, with the goal in view.'],['Walk Away','Protect what matters by leaving the heat.'],['Ask','Turn the unknown into information.'],['Delegate','Use shared power instead of carrying everything alone.']];export const DecisionChamber:React.FC=()=>{const[c,setC]=useState<string|null>(null);return <div className="grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><div className="rounded-[2rem] border border-amber-300/20 bg-black/25 p-6 md:p-8"><p className="text-xs uppercase tracking-[.2em] text-amber-300/55">A real-life moment</p><h3 className="mt-3 font-serif text-2xl text-amber-50">Someone takes credit for work you did.</h3><div className="mt-7 grid grid-cols-2 gap-3 md:grid-cols-3">{o.map(([n])=><motion.button whileTap={{scale:.97}} key={n} onClick={()=>setC(n)} className="rounded-2xl border border-amber-300/15 bg-amber-950/15 px-4 py-4 text-sm text-amber-50 hover:border-amber-300/45">{n}</motion.button>)}</div></div><div className="min-h-64 rounded-[2rem] border border-amber-300/15 bg-black/25 p-6 md:p-8"><AnimatePresence mode="wait">{c?<motion.div key={c} initial={{opacity:0,y:15}} animate={{opacity:1,y:0}}><p className="text-xs uppercase tracking-[.2em] text-amber-300/55">You chose</p><p className="mt-2 font-serif text-3xl text-amber-100">{c}</p><p className="mt-4 text-sm leading-7 text-amber-100/60">{o.find(x=>x[0]===c)?.[1]}</p></motion.div>:<div><p className="font-serif text-2xl text-amber-50/80">Between stimulus and response, there is space.</p><p className="mt-4 text-sm leading-7 text-amber-100/45">Notice that space before you decide.</p></div>}</AnimatePresence></div></div>};
