import React,{useState}from'react';import{motion}from'framer-motion';import{WorldStage,ChoicePill}from'../components/WorldStage';
export const DecisionLabSection:React.FC=()=>{const[choice,setChoice]=useState(0);const opts=['"hold"', '"prepare"', '"speak"', '"walk"'];return <WorldStage world={48} eyebrow="World 19" title="A real choice in the forge" subtitle="Try four responses to the same situation and compare their downstream costs." image="human"><div className="grid md:grid-cols-4 gap-3">{opts.map((o,i)=><button key={o} onClick={()=>setChoice(i)} className={`min-h-40 rounded-3xl border p-5 text-left ${choice===i?'border-amber-300/50 bg-amber-300/10':'border-white/10 bg-black/20'}`}><div className="text-[10px] tracking-[.28em] uppercase text-white/30">0{i+1}</div><div className="mt-10 text-2xl font-serif">{o}</div></button>)}</div><motion.div key={choice} initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} className="mt-8 rounded-3xl border border-amber-300/15 bg-black/20 p-7 text-white/55">Your chosen move changes the downstream scene. The fire follows what you actually own—not what you wish you could control.</motion.div></WorldStage>}
