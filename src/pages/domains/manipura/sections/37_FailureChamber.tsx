import React,{useState}from'react';import{motion}from'framer-motion';import{WorldStage}from'../components/WorldStage';
export const FailureChamberSection:React.FC=()=>{const[items,setItems]=useState(Array.from({length:5},(_,i)=>i));return <WorldStage world={37} eyebrow="World 8" title="Break the outcome, keep the learning" subtitle="Turn fragments of a failed plan into the next attempt." image="forge"><div className="min-h-[390px] rounded-[2rem] border border-white/10 bg-black/25 p-8 flex flex-wrap items-center justify-center gap-5">{items.map(i=><motion.button layout key={i} onClick={()=>setItems(a=>a.filter(x=>x!==i))} className="w-28 h-28 rounded-3xl border border-amber-300/20 bg-gradient-to-br from-amber-200/20 to-black flex items-end p-4 text-left"><span className="font-serif text-lg">{['skill','role','result','belief','habit'][i%5]}</span></motion.button>)}{items.length===0&&<div className="text-center"><div className="text-5xl font-serif text-amber-100">What remains?</div><p className="mt-4 text-white/45">Something quieter than the outcome.</p></div>}</div></WorldStage>}
