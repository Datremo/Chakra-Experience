import React,{useState}from'react';import{motion}from'framer-motion';import{WorldStage}from'../components/WorldStage';
export const ControlToAgencySection:React.FC=()=>{const[done,setDone]=useState(false);const[step,setStep]=useState(0);return <WorldStage world={72} eyebrow="World 43" title="Control → agency" subtitle="Release the outer objects; keep the center moving." image="human"><div className="rounded-[2rem] border border-white/10 bg-black/25 p-8 md:p-12"><div className="flex items-center gap-2 mb-8">{Array.from({length:5}).map((_,i)=><button key={i} onClick={()=>setStep(i)} className={`h-2 flex-1 rounded-full ${i<=step?'bg-amber-300':'bg-white/10'}`}/> )}</div><motion.div key={step} initial={{opacity:0,x:20}} animate={{opacity:1,x:0}} className="min-h-48 flex items-center justify-center text-center"><div><div className="text-5xl font-serif text-amber-100">{['Notice','Name','Choose','Act','Integrate'][step]}</div><p className="mt-4 text-white/50 max-w-2xl">{['Start with what is actually happening.','Give the pattern a precise name.','Choose one response you own.','Take the smallest useful action.','Let the result teach you.'][step]}</p></div></motion.div><button onClick={()=>{if(step<4)setStep(step+1);else setDone(true)}} className="mt-8 mx-auto block px-6 py-3 rounded-full bg-amber-300 text-black">{step<4?'Next':'Seal the insight'}</button>{done&&<div className="mt-6 text-center text-amber-100/70">You completed the loop. Return to the fire with one concrete change.</div>}</div></WorldStage>}
