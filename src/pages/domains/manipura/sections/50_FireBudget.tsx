import React,{useMemo,useState}from'react';import{WorldStage}from'../components/WorldStage';
export const FireBudgetSection:React.FC=()=>{const[spent,setSpent]=useState<number[]>([]);const cats=['body','deep work','people','admin','rest'];const total=24;const used=spent.reduce((a,b)=>a+b,0);return <WorldStage world={50} eyebrow="World 21" title="Your day has finite heat" subtitle="Spend 24 units across deep work, people, body, admin, and rest." image="dawn"><div className="grid md:grid-cols-5 gap-3">{cats.map((c,i)=><button key={c} onClick={()=>setSpent(a=>[...a,i])} className="rounded-3xl border border-white/10 bg-black/20 p-6 min-h-44 text-left"><div className="text-xs uppercase tracking-[.25em] text-white/30">{i+1}</div><div className="mt-10 font-serif text-xl">{c}</div><div className="mt-3 text-3xl text-amber-100">{spent.filter(x=>x===i).length}</div></button>)}</div><div className="mt-8 text-center text-white/45">{used} / {total} heat units allocated. {used>total?'You are borrowing from tomorrow.':'You still have room.'}</div></WorldStage>}
