import React,{useState}from'react';import{motion}from'framer-motion';import{WorldStage,GlowOrb}from'../components/WorldStage';
export const FireToAirSection:React.FC=()=>{const[p,setP]=useState(0);return <WorldStage world={74} eyebrow="World 45" title="Fire makes room for breath" subtitle="The world opens. A current pulls toward Anāhata." image="horizon" tone="light"><div className="min-h-[430px] rounded-[2rem] border border-amber-100/10 bg-black/15 relative overflow-hidden flex items-center justify-center"><motion.div animate={{scale:1+p*.08,opacity:1-p*.06}} className="absolute"><GlowOrb size={260}/></motion.div><motion.div animate={{x:p*60,y:-p*18,opacity:1-p*.12}} transition={{duration:1.5}} className="relative z-10 text-center"><div className="text-5xl md:text-7xl font-serif text-white">{p<1?'Fire':p<2?'Clarity':'Open air'}</div><p className="mt-5 text-white/50">{p<1?'Warmth, force, transformation.':p<2?'The same energy, now illuminating.':'The next centre makes room for breath.'}</p></motion.div><button onClick={()=>setP(x=>Math.min(3,x+1))} className="absolute bottom-7 px-6 py-3 rounded-full bg-amber-200 text-black">Continue</button></div></WorldStage>}
