import React,{useEffect,useState}from'react';import{motion}from'framer-motion';import{WorldStage}from'../components/WorldStage';
export const StabilitySection:React.FC=()=>{const[t,setT]=useState(0);useEffect(()=>{const id=setInterval(()=>setT(x=>(x+1)%8),700);return()=>clearInterval(id)},[]);return <WorldStage world={61} eyebrow="World 32" title="Strength can be quiet" subtitle="Balance a central flame with tiny posture-like adjustments." image="human"><div className="relative min-h-[400px] rounded-[2rem] border border-white/10 bg-black/25 flex items-center justify-center overflow-hidden"><motion.div animate={{scale:0.85+t/35,opacity:[.7,1,.7]}} transition={{duration:2,repeat:Infinity}} className="w-40 h-40 rounded-full bg-[radial-gradient(circle,rgba(255,250,220,1),rgba(245,158,11,.75)_28%,transparent_72%)]"/>{Array.from({length:8}).map((_,i)=><motion.div key={i} animate={{opacity:i===t?1:.15,scale:i===t?1.2:.9}} className="absolute bottom-8 w-8 h-8 rounded-full border border-amber-300/20" style={{left:`${8+i*11}%`}}/> )}<button onClick={()=>setT((t+1)%8)} className="absolute bottom-7 right-7 px-5 py-3 rounded-full border border-white/10">Keep it alive</button></div></WorldStage>}
