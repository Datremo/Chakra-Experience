import React from 'react';
import { WorldFrame } from '../components/WorldFrame';
import { TruthEngine } from '../components/TruthEngine';

export const TruthEngineWorldSection: React.FC = () => <WorldFrame eyebrow="28 — DISCERNMENT" title="Can the story survive the light?" copy="Separate what a tradition describes, what later interpreters added, what symbolism can mean, and what evidence actually supports." scene="temple"><TruthEngine /></WorldFrame>;
